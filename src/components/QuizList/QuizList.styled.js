import styled from 'styled-components';

export const List = styled.ul`
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
`;

export const ListItem = styled.li`
  max-width: 300px;
  padding: 5px;
`;
